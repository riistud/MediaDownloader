import { db } from "@db";

export const storage = {
  // Storage operations would go here if we needed to store anything in the database
  // Currently we don't need database storage for this application as we're just proxying API requests
};
